# Copy to infra/identity/.env.identity (ignored by Git) and generate every
# value independently, for example: openssl rand -base64 48
HYDRA_POSTGRES_PASSWORD=
HYDRA_SYSTEM_SECRET=
HYDRA_COOKIE_SECRET=
OIDC_TOKEN_HOOK_SECRET=
OIDC_LOGOUT_BROKER_SECRET=
JIEYA_OIDC_CLIENT_SECRET=
