# StarStack 部署/更新方法（PM2 + Nginx）

适用场景：
- 服务器路径：`/home/user/star-stack`
- 使用 `PM2` 管理后端进程
- 使用 `Nginx` 提供前端静态资源与反向代理

本文档基于你当前实际使用流程整理，并补充了必要的安全说明（避免误改 `server/index.js` 启动逻辑）。

## 一次性配置（后端监听地址）

你原来的习惯是每次拉取代码后手改 `server/index.js` 末尾监听配置。  
当前版本仓库中，`server/index.js` 末尾包含：
- `initDb()`
- `scheduleLeaderboardHistory()`
- `saveLeaderboardHistory()`

不要直接把整段替换成一个简单的 `app.listen(...)`，否则会丢失数据库初始化和排行榜任务。

正确做法是：保留现有启动链，只把监听端口/地址改成生产可用形式。

建议改成（示意）：

```js
const PORT = Number(process.env.PORT) || 3000
initDb()
  .then(() => {
    app.listen(PORT, '0.0.0.0', () => {
      console.log(`Server running on port ${PORT}`)
      scheduleLeaderboardHistory()
      saveLeaderboardHistory()
    })
  })
  .catch((error) => {
    console.error('Failed to init database:', error)
    process.exit(1)
  })
```

说明：
- `0.0.0.0`：允许外部访问（配合 Nginx/防火墙）
- 默认端口 `3000`：与 `PM2` 生产环境配置一致（`ecosystem.config.js`）

如果你已经改好这一步，后续部署时不需要每次重复改。

## 日常更新部署步骤（标准流程）

### 1. 进入项目目录

```bash
cd /home/user/star-stack
```

### 2. 备份数据库（强烈建议每次更新前执行）

```bash
cp server/data/starstack.sqlite server/data/starstack.sqlite.backup.$(date +%Y%m%d_%H%M%S)
```

### 3. 拉取最新代码

```bash
git pull
```

### 4. 安装依赖（前后端）

注意：根目录 `npm install` 只安装前端依赖，后端依赖在 `server/` 目录。

```bash
# 前端依赖（根目录）
npm install

# 后端依赖（server 目录）
cd server && npm install && cd ..
```

如果本次更新没有改依赖，也可以跳过这一步。

### 5. 构建前端生产文件

```bash
npm run build
```

### 6. 重启服务（PM2）

你当前用法：

```bash
pm2 restart all
```

更推荐（避免误重启其他服务）：

```bash
pm2 restart star-stack-api
```

### 7. 重载 Nginx

你当前用法：

```bash
sudo nginx -s reload
```

更稳妥（先检查配置再重载）：

```bash
sudo nginx -t && sudo nginx -s reload
```

## 一条龙命令清单（按顺序执行）

```bash
cd /home/user/star-stack
cp server/data/starstack.sqlite server/data/starstack.sqlite.backup.$(date +%Y%m%d_%H%M%S)
git pull
npm install
cd server && npm install && cd ..
npm run build
pm2 restart all
sudo nginx -t && sudo nginx -s reload
```

## 部署后检查（建议）

```bash
pm2 status
pm2 logs star-stack-api --lines 50
```

建议检查项：
- 页面可正常打开
- `/api` 请求正常
- 提交评测功能正常
- 数据库数据未丢失
- 排行榜/消息/讨论区接口返回正常

## 回滚（数据库）

如果更新后出现数据库异常，可用备份恢复（将时间戳替换为实际文件名）：

```bash
cd /home/user/star-stack
cp server/data/starstack.sqlite.backup.YYYYMMDD_HHMMSS server/data/starstack.sqlite
pm2 restart star-stack-api
```

## 常见注意事项

- `npm run build` 是前端构建，后端不需要构建。
- `pm2 restart all` 会重启服务器上所有 PM2 进程，谨慎使用。
- 若 `git pull` 后 `server/index.js` 被覆盖，检查监听是否仍为生产配置（`PORT=3000` / `0.0.0.0`）。
- 若 Nginx 配置改动过，务必先 `nginx -t` 再 `reload`。
