# Cloudflare Turnstile public Site Key, embedded into the Vite frontend at build time.
VITE_TURNSTILE_SITE_KEY=

# 服务器邮件配置（不要放入前端 VITE_ 变量；生产环境通过 PM2 环境注入）
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=StarStack <your-email@example.com>
