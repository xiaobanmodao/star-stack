# StarStack 开发审查制度

> 目标：保证开发出来的内容不能出现错误。本文档是每次改动的强制流程。

## 零、开发前讨论制度（最高优先级，不可违反）

**任何开发动作，必须先讨论、后动手、经确认再实施。**

### 流程（每次收到开发需求时）

1. **先复述需求**：用自己的话确认"要做什么、不要做什么"，包括明确排除项（例：用户说"侧边栏给 OJ、主页不要"，就要同时确认 OJ 有侧栏、主页无侧栏）
2. **梳理方案**：说明实现思路、涉及的文件与模块、影响范围、备选方案及其取舍
3. **等用户同意**：得到明确同意后才开始写代码；用户没表态 = 不动手
4. **动手后按审查制度交付**（审计/双主题/验证）

### 触发场景

- 新功能开发 / 功能修改 / 功能移除：**必须**走完整讨论流程
- 结构调整（导航/布局/路由/主题）：**必须**走完整讨论流程
- 明确 bug 修复（用户指出具体错误现象）：先说明修复方案（一两句话）再动手，修完汇报
- 极端紧急情况（如服务宕机）：可直接修复，但事后必须补说明

### 反面教材（为什么要立这条规矩）

侧边栏功能反复返工 4 轮：先做全局侧栏 → 按建议改成"主站无侧栏 + OJ 内导航" → 用户要求删 OJ 导航恢复贴边侧栏（误改为全局）→ 用户澄清"只要 OJ 有侧栏、主页不要"。
**每一轮都差在"没有先确认清楚就动手"**——多花数轮、浪费精力与费用。

### 讨论时必带的信息

- 需求理解（含排除项）
- 方案与文件影响面
- 备选方案（如有）
- 风险点（如涉及现有功能）


## 一、铁律（不可违反）

1. **设计令牌优先**：所有颜色必须使用 `styles/tokens.css` 的变量（`--ss-*`），**禁止硬编码颜色**（hex / rgba）。需要新颜色时先加令牌，再使用。
2. **双主题必须同时正确**：任何 UI 改动默认要过浅色 + 深色两个主题。`data-theme='light'` 覆盖块在 `App.css` 末尾统一维护，新页面/新组件不得另起炉灶。
3. **文字反馈方向**：暗色主题文字要亮、点击不能变暗；亮色主题文字要暗、点击不能变亮。hover/active 只允许向"更清晰"方向变化。
4. **禁止叠加多套样式体系**：App.css 已有多轮改版的历史包袱（classic / liquid glass / final 三层）。新样式只写进对应页面 CSS 或 final 覆盖块，不得再叠新层。
5. **交互反馈克制**：禁止渐变、禁止大位移动画，hover 只用背景/边框微变。

## 二、改动分级与审查要求

| 级别 | 改动范围 | 审查要求 |
|---|---|---|
| S | 文案 / 数值 / 纯逻辑（无 UI） | tsc + eslint + build |
| A | 颜色 / 间距 / 单个组件样式 | S 级 + grep 硬编码颜色检查 |
| B | 页面布局 / 组件结构 / 新页面 | A 级 + 运行 `npm run audit`（双主题审计 0 违规）+ 检查遮挡/溢出 |
| C | 主题系统 / 全局结构 / 路由 / 骨架 | B 级 + 全页面回归（`npm run audit` 全量）+ 移动端断点检查 |

## 三、交付前检查清单（每次交付附上）

- [ ] `npx tsc -b` 通过
- [ ] `npx eslint src/ server/` 通过
- [ ] `npm run build` 通过
- [ ] `npm run audit` 双主题 0 违规（或 B 级以上改动必跑）
- [ ] 无新增硬编码颜色（`grep -rn "#[0-9a-f]\{6\}\|rgba(" src --include="*.css" | grep -v tokens` 复查）
- [ ] 无新增 z-index 冲突 / 无 overflow 裁剪导致显示不全
- [ ] hover/active 方向正确（暗色变亮、亮色变暗）
- [ ] 未绕过令牌直接写死色值

## 四、审计工具

```bash
npm run audit              # 双主题 + 9 个核心页面
npm run audit:light        # 只审浅色
npm run audit:dark         # 只审深色
npm run audit:custom /oj/x # 追加自定义页面
```

- 工具：`scripts/audit.mjs`（真实 Chrome 渲染，遍历每个可见文字元素计算对比度）
- 报告：`.audit/audit-*.md`
- 退出码 0 = 通过，1 = 违规（可接入 CI）

## 五、历史事故档案（教训来源）

| 事故 | 根因 | 防再犯规则 |
|---|---|---|
| 浅色全站亮色字 | legacy 变量（`--text/--muted/--panel`）被 `:root` 硬编码为深色值，light 块未覆盖 | 铁律 1/2：改主题系统必须双主题全页面回归 + 审计 |
| 私信输入框看不见 | 全局锁滚动 + 高度硬算 | C 级改动全页面回归 |
| 卡片点击边框消失 | hover 边框透明度 + 按钮 tap 高亮未重置 | B 级检查交互反馈 |
| 难度徽标变灰 | 可读性覆盖组误伤彩色语义色 | 语义色（难度/状态）必须保留彩色，只加深不抹色 |

## 六、开发流程

1. 小步提交：一次改动一个主题（feat:/fix:/refactor: 中文描述）
2. 改完按分级跑审查
3. 交付附上检查清单结果
4. 涉及 C 级改动，先讨论方案再动手

## 七、事故档案 2：首页卡片边框消失（深刻教训）

**现象**：`<button>` 卡片 hover/点击边框消失，`<a>` 卡片正常，反复 4 轮未修复。

**根因**：App.css 超长 hover 选择器组含裸 `button:hover` + `!important`，用 `--hover-calm`（硬编码白色，浅色无覆盖）盖掉所有按钮的 hover 边框/背景。

**为什么花了 4 轮（流程检讨）**：
1. 前两轮纯静态推理"猜"修复，未用真实渲染验证
2. 第三轮 CDP 已拿到决定性证据（按钮 hover 白色、链接星蓝），却误判为"CDP 模拟缺陷"自我合理化，没有追查
3. 静态 grep 漏掉超长选择器组里的裸 `button:hover`
4. 前几轮修复未做层叠优先级检查，全被 `!important` 压掉

**防再犯规则（新增强制项）**：
- [ ] **视觉问题一律先开 CDP 拿计算样式**，禁止纯静态推理猜测
- [ ] **反常识实测数据 = 重大线索**：实测与预期不符时，必须追查到来源规则（`CSS.getMatchedStylesForNode`），禁止自我合理化
- [ ] **A 正常 / B 异常时，先列 A/B 的差异**（DOM/匹配规则/伪类），从差异出发排查
- [ ] **写任何新规则前检查层叠全链**：是否会被 `!important` / 更高特异性 / 后加载规则压过（grep 时注意超长选择器组里的裸标签选择器）
- [ ] 交互样式（hover/active）必须分别验证 button 与 a 两种元素
